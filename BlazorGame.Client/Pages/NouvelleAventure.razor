@page "/nouvelle-aventure"
@inject NavigationManager Navigation

<PageTitle>Nouvelle Aventure</PageTitle>

<div class="simple-adventure-page">
    <div class="adventure-header">
        <h1>🚀 Nouvelle Aventure</h1>
        <p>Choisissez comment commencer votre quête</p>
    </div>

    <div class="adventure-options">
        <!-- Option 1 : Aventure Guidée -->
        <div class="option-card">
            <div class="option-icon">🎮</div>
            <h3>Commencer l'Aventure</h3>
            <p>Démarrez directement dans le donjon et affrontez vos premiers ennemis</p>
            <button class="btn-option" @onclick="CommencerAventure">
                Entrer dans le donjon
            </button>
        </div>

        <!-- Option 2 : Voir le tutoriel -->
        <div class="option-card">
            <div class="option-icon">📚</div>
            <h3>Guide du Débutant</h3>
            <p>Apprenez les bases du combat et de l'exploration</p>
            <button class="btn-option btn-secondary" @onclick="VoirTutoriel">
                Voir le tutoriel
            </button>
        </div>

        <!-- Option 3 : Paramètres -->
        <div class="option-card">
            <div class="option-icon">⚙️</div>
            <h3>Paramètres</h3>
            <p>Configurez les options de jeu et la difficulté</p>
            <button class="btn-option btn-secondary" @onclick="OuvrirParametres">
                Ouvrir les paramètres
            </button>
        </div>
    </div>

    <!-- Message de statut -->
    @if (!string.IsNullOrEmpty(MessageStatut))
    {
        <div class="status-message">
            @MessageStatut
        </div>
    }
</div>

@code {
    private string MessageStatut = "";

    private void CommencerAventure()
    {
        // Redirige vers la page Salle.razor qui est dans le dossier Components
        Navigation.NavigateTo("/salle");
    }

    private void VoirTutoriel()
    {
        MessageStatut = "#################################################";
    }

    private void OuvrirParametres()
    {
        MessageStatut = "⚙️ Les paramètres seront disponibles dans une future mise à jour.";
    }

    private void RetourAccueil()
    {
        Navigation.NavigateTo("/");
    }
}

<style>
    .simple-adventure-page {
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
        min-height: 80vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .adventure-header {
        text-align: center;
        margin-bottom: 3rem;
    }

    .adventure-header h1 {
        color: #ffd700;
        font-size: 2.5rem;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
    }

    .adventure-header p {
        color: #e0d0b0;
        font-size: 1.2rem;
        margin: 0;
    }

    .adventure-options {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-bottom: 2rem;
    }

    .option-card {
        background: 
            linear-gradient(rgba(40, 25, 15, 0.8), rgba(30, 15, 5, 0.8));
        background-size: cover;
        padding: 2rem;
        border-radius: 15px;
        border: 2px solid #8b4513;
        text-align: center;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5);
        transition: all 0.3s ease;
    }

    .option-card:hover {
        transform: translateY(-5px);
        border-color: #d4af37;
        box-shadow: 0 10px 25px rgba(212, 175, 55, 0.3);
    }

    .option-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
    }

    .option-card h3 {
        color: #ffd700;
        margin-bottom: 1rem;
        font-size: 1.4rem;
    }

    .option-card p {
        color: #e0d0b0;
        margin-bottom: 1.5rem;
        line-height: 1.5;
    }

    .btn-option {
        width: 100%;
        padding: 1rem 2rem;
        background: linear-gradient(135deg, #d4af37, #b8860b);
        color: #1a0f0f;
        border: none;
        border-radius: 8px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
        font-family: 'MedievalSharp', cursive;
        font-size: 1rem;
    }

    .btn-option:hover {
        background: linear-gradient(135deg, #b8860b, #d4af37);
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(212, 175, 55, 0.4);
    }

    .btn-secondary {
        background: transparent;
        color: #d4af37;
        border: 2px solid #d4af37;
    }

    .btn-secondary:hover {
        background: rgba(212, 175, 55, 0.1);
        color: #ffd700;
    }

    .status-message {
        background: rgba(212, 175, 55, 0.2);
        border: 1px solid #d4af37;
        color: #ffd700;
        padding: 1rem;
        border-radius: 8px;
        text-align: center;
        margin-top: 2rem;
        font-weight: bold;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .simple-adventure-page {
            padding: 1rem;
        }

        .adventure-header h1 {
            font-size: 2rem;
        }

        .adventure-options {
            grid-template-columns: 1fr;
        }
    }
</style>